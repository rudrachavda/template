# Design System & Code Guidelines

Ported from `bento` (personal portfolio) — the design language this template
is built on. This file, not `AGENTS.md`, is where the design rules live:
`AGENTS.md` is regenerated by `next dev` itself (see the marked block inside
it) and shouldn't be hand-edited for this.

**Standing principle**: this is a template for *any* web app, not a
portfolio. Visual language (colors, type, shape, motion) ports over as-is —
that's the point — but a component's *behavior* shouldn't assume portfolio
content (an always-external link, a fixed "back to my projects" target, a
hardcoded page title). When porting or adding something to `components/`,
keep the site-specific parts (an href, a label, what icon) as props supplied
by the call site, and keep the shared component itself general. See §6 for
the concrete example (`Button`).

## 1. Color System

Three-tier surface hierarchy, each tier a light/dark pair, defined as CSS vars
in `app/globals.css` `@theme`:

| Tier | Light | Dark | Used for |
|---|---|---|---|
| Background | `#fefefe` | `#121212` | `<body>` |
| Primary surface | `#fafafa` | `#1d1d1d` | Card fills |
| Secondary surface | `#f0f0f0` (or `#fff`) | `#171717` | Nested/secondary panels |

Hairline border, on every card/bordered surface, `border-[0.5px] border-solid`:
```
border-[rgba(225,228,232,0.8)] dark:border-[rgba(29,29,29,0.8)]
```
If nesting a border inside another border, a plain `border` (no explicit
width) avoids doubled hairlines.

Persistent chrome (a header/nav bar, a footer) should use the **secondary
surface** tint (`bg-[#f0f0f0] dark:bg-[#171717]`), not the plain background —
otherwise it reads as flush with the page content instead of a distinct band
of UI. Reserve the plain background for the actual content area.

Text colors (`#1d1d1d`/`#f0f0f0` pair — **not** `zinc-100`, that's a
button-only idiom, see §4):
- Primary text: `#1d1d1d` light / `#f0f0f0` dark, always paired with `dark:antialiased`.
- Secondary text: `#646464` (subtitle/label) or `#a1a1a1` (secondary/muted — dates, numbers).
- Selection highlight: `background: #ededed; color: #1d1d1d`.

Accent palette (gradients, status colors — not surface colors):
```
#9659b9 #ee98b7 #1a2b3b #0069cc #50aef6 #abcdef #169b40 #0a461d #fcd609 #fc9601 #b64400 #fc3e2f
```
`#1a2b3b` doubles as the default `Shadow` component tint. `#169b40` is the
success green. `#0088ff` is the inline-link blue.

## 2. Typography

Self-hosted **Inter Variable** (`public/fonts/Inter/InterVariable.ttf` via
`next/font/local`, not `next/font/google`), optical size locked to text mode
(`font-variation-settings: "opsz" 14`), with a curated feature-setting list.
`font-mono` is the system UI-monospace stack. `SpaceGrotesk`/`HaloGrotesk` are
registered as `@font-face`s but not wired into any component yet — available,
currently dormant.

The whole system runs on **one size**: `text-sm`, almost always `font-medium`,
`tracking-normal`. Variation comes from color and leading, not size — see the
`Text` component (`components/typography/text-styles.tsx`):

| Role | Classes |
|---|---|
| Title | `text-sm leading-snug font-medium tracking-normal text-pretty text-[#1d1d1d] dark:text-[#f0f0f0] dark:antialiased` |
| Subtitle | `text-sm leading-snug font-medium tracking-normal text-pretty text-[#646464]` |
| Paragraph | `text-sm leading-relaxed font-medium tracking-normal text-[#1d1d1d] md:text-pretty dark:text-[#f0f0f0] dark:antialiased` |
| Secondary | `text-sm leading-relaxed font-medium tracking-normal text-[#a1a1a1] md:text-pretty` |
| Label | `text-sm leading-relaxed font-medium tracking-wide text-[#646464] uppercase md:text-pretty` |

`components/typography/header.tsx` is the one deliberate exception
(`font-[590] leading-5.25`) — reserved for section headers, don't reuse the
weight elsewhere. Its `className` prop only reaches the outer wrapper row
(layout/spacing) — the title/date text have their own `titleClassName`/
`dateClassName` for overriding font-size/weight/etc, same idea as `Text`'s
`className` but as two explicit slots instead of one, since Header renders
two independently-styled text elements, not one.

Numbers/data columns: `font-mono` with `tabular-nums` for alignment.

**tailwind-merge gotcha**: `cn()`/`twMerge()` treats a `text-*` (font-size)
utility as conflicting with `leading-*` — a `text-*` that comes *after* a
`leading-*` in the merged string silently strips it. Always write
`text-sm leading-relaxed`, never the reverse order.

## 3. Layout & Spacing

Reading column width: `max-w-173` (~692px), via `Section`
(`components/layout/section.tsx`, `variant='restricted'` is the default —
`mx-auto w-full max-w-173 px-6 flex`). `variant='full'` drops the constraint
for hero/screenshot sections that should bleed edge to edge.

Chronological/data lists use a fixed-label + flexible-content grid:
`grid-cols-[3.75rem_1fr] md:grid-cols-[6.75rem_1fr]`.

**4px grid**: every spacing/sizing value — padding, margin, gap, width/height,
inset — should land on a multiple of 4px (Tailwind's `1`–`96` scale already
*is* this grid: `p-1`=4px, `p-2`=8px, `p-4`=16px, `size-9`=36px, etc., so
sticking to whole Tailwind steps gets this for free). The one allowed
exception is **6px** (Tailwind's `1.5` step — `py-1.5`, `gap-1.5`), used for
tight rhythm inside compact controls. Half-steps other than that (`0.5`=2px,
`2.5`=10px, `3.5`=14px, `4.5`=18px) and arbitrary pixel values (`gap-[10px]`,
`mr-[3px]`) don't comply — round to the nearest 4px step instead (an
in-between icon size like 18px becomes `size-4`(16px) or `size-5`(20px), not
`size-4.5`).

**Scrollbars are hidden, matching `bento`**: a `.no-scrollbar` utility class
exists (`app/globals.css`) for one-off opt-in cases, but the actual reason no
scrollbar ever shows is a global rule — `* { scrollbar-width: none }` plus
`*::-webkit-scrollbar { display: none }` — copied from `bento/app/globals.css`.
Applying `.no-scrollbar` to just `<body>` doesn't hide it reliably: when
`<body>` has no constrained height, the page's real scroll container is
`<html>`, not `<body>`, and a class on the wrong element does nothing. The
global `*` rule sidesteps having to know which element ends up scrolling.

## 4. Shape Language — Squircle

`components/ui/squircle/` renders a true superellipse via SVG path, not CSS
`border-radius`. Two fixed idioms — don't invent a third without reason:
- **Card/button squircle**: `radius={12} exponent={5} smoothing={1}`.
- **Pill/circle**: `radius={999} exponent={2} smoothing={1}` (true circular math).

`smoothing` is always `1`. `squircle/tokens.ts`'s `radiusFor(height)` formula
(`round(height * 0.42)`) is still there for anything that wants a radius
proportional to its own height, but `Button` itself doesn't use it — its
`radius`/`exponent`/`smoothing` are direct, independently-set props (default
10/5/1) that don't key off height at all, the same way plain CSS
`border-radius` doesn't scale with element size either. See §6.

**Rule**: never pair a `Squircle` with a Tailwind `shadow-*`/`box-shadow` class
— the shadow cuts inside the squircle's real edge. Use `filter: drop-shadow(...)`
instead, since it traces the rendered alpha silhouette. See §5.

## 5. Shadows

Two separate, non-interchangeable systems — pick by what's casting the shadow:

- **`Shadow` component** (`components/ui/shadow.tsx`) — for plain
  `border-radius` surfaces. Five-layer stack, scaled by `elevation`/
  `intensity`, tinted by `hexColor` (default `#1a2b3b`), applied as an inline
  `boxShadow` (so a `dark:shadow-*` Tailwind class won't touch it — pass a
  `darkHexColor` prop if it needs to differ by theme). Its light-mode inner
  highlight ring is written but commented out in the source — dead code, not
  a missing feature to restore casually.
- **`drop-shadow(...)`** — required for anything shaped by `Squircle`, since
  `box-shadow` is clipped to a `border-radius` arc, not the real superellipse
  edge. The card idiom disables it entirely in dark mode, not just dims it:
  ```
  [filter:drop-shadow(0_1px_1px_rgba(28,29,33,0.13))_drop-shadow(0_6px_7px_rgba(28,29,33,0.07))]
  dark:[filter:none]
  ```
  For the layered, `elevation`/`intensity`/`hexColor`-tunable version of that
  same idea (rather than one hand-picked pair of values), use
  `getLayeredDropShadow(hexColor, elevation, intensity)` — exported
  alongside `getLayeredShadow` from `components/ui/shadow.tsx`, same
  signature and tuning knobs, just emitting `drop-shadow(...)` layers
  instead of `box-shadow` ones (see `/components/shadow`'s own demo tiles,
  which use it on real `Squircle`s). Deliberately only 3 layers, not
  `getLayeredShadow`'s 5: `drop-shadow` is a real per-pixel alpha-blur
  filter, not a cheap compositor `box-shadow` — meaningfully more expensive
  to paint, and the two heaviest tiers (70px/192px offsets) were sized for
  a large decorative marketing card, not a card-sized UI surface. Disable
  it in dark mode the same way as the hand-picked version above — the
  hairline stroke on the `Squircle` is what gives it an edge there instead.

## 6. Buttons & Interactive Components (`components/ui/buttons.tsx`)

`Button` is a **general-purpose** primitive — any button anywhere, not a
component fine-tuned for one site's social-link row. Every clickable shape
still paints its own surface as a `Squircle` with `fill="currentColor"` (the
wrapper's text-color class *is* its background, label/icon sit in a
`relative z-10` layer above it), but the API is generic:

- **`variant`**:
  - `solid` — a real, always-visible inverted surface, the primary-action
    look: `text-[#1d1d1d] hover:text-zinc-700 dark:text-[#f0f0f0]
    dark:hover:text-zinc-300`, label always the opposite tone
    (`text-[#f0f0f0] dark:text-[#1d1d1d]`) and constant, not hover-flipped.
    Deliberately **not** the old `zinc-100`/`zinc-950` icon-button idiom —
    `zinc-100` sits almost indistinguishably close to the `#fefefe` page
    background at rest, reading as an invisible button until hovered. That's
    fine for a quiet icon-only control (see `ICON_LABEL` below), wrong for
    the one variant meant to be the obvious primary action.
  - `outline` — transparent + a real `Squircle` `stroke` using the
    `--border-hairline` CSS var, so it flips with `.dark` without a
    `useTheme()` round-trip.
  - `ghost` — transparent, no border, tints on hover only.
  - `main`/`primary`/`secondary` — the three **configurable color tiers**.
    Each is backed by a `--color-button-*` custom property in
    `globals.css` (`--color-button-main`/`-main-hover`/`-main-label`/
    `-main-label-hover`, and the same set for `primary`/`secondary`)
    rather than a hardcoded hex in `buttons.tsx`, referenced as
    `text-[var(--color-button-main)]` etc. Every one of these vars is
    redefined inside the `.dark` block (same mechanism as
    `--border-hairline`), so **one class covers both themes** — no
    `dark:` variant class needed on these three, unlike `solid`/`outline`/
    `ghost`. Re-branding every button in the app — swapping the accent
    color, lightening the secondary surface, whatever — is a one-file
    edit to `globals.css`, not a `buttons.tsx` change. `secondary` is the
    flat secondary-surface tier from §1 (`#f0f0f0`/`#171717`), the *same*
    tone in both themes rather than an inversion, paired with a border by
    default (see `border` below). `primary` is the near-white/near-black
    surface tier (`#fafafa`/`#1d1d1d`), no border by default. `main` is
    the brand/CTA accent (defaults to accent blue, `#0069cc` light /
    `#50aef6` dark — lighter in dark mode for contrast against a dark
    page, same idea as the `darkBackground` example further down) — for
    the one or two buttons per page that should visually stand out from
    everything else, not a default to reach for on every button.
    **`secondary` is the default for functional/real buttons in this
    project** — the navbar CTA, the hero's buttons, `Back`/`Copy`/
    `ThemeToggle` are all `variant="secondary"`. `solid`/`outline`/`ghost`/
    `main` exist and are demoed on `/components/buttons` for range, but reach for
    `secondary` first for anything actually shipped, unless there's a
    specific reason (a hero wanting one strong CTA, say) to do otherwise.
  - `unstyled` — no `VARIANT_SURFACE` entry and, by default, **no
    `Squircle` at all**: not a transparent surface still taking up a DOM
    node, genuinely nothing painted, just the bare label/icon plus the
    usual press/focus feedback. For an affordance that shouldn't read as
    a button until interacted with (an inline copy-link glyph next to
    plain text, say). Pass `border` explicitly and it reappears as a
    stroke-only outline (`fill="none"`, so it doesn't accidentally
    inherit an ambient `currentColor` fill); pass `background` and it
    reappears filled with that literal color. Label color falls back to
    the plain body-text pair (`text-[#1d1d1d] dark:text-[#f0f0f0]`) for
    non-icon sizes; icon sizes use the usual `ICON_LABEL` muted-to-full-
    contrast treatment regardless of variant, same as every other variant.
- **Shape is a first-class prop, not a closed set of two named variants.**
  `radius`/`exponent`/`smoothing` pass straight through to the underlying
  `Squircle` (defaults `10`/`5`/`1` — the "classic squircle" soft-rounded
  rect). Want a pill? `radius={999} exponent={2}` (or the `pill` shorthand,
  below). Want a barely-rounded corner, or something boxier? Pass whatever
  `radius`/`exponent` you actually want — there's no enum gate to work
  around. `strokeWidth` is exposed the same way for the `outline` variant's
  border.
- **`pill`**: shorthand for `radius={999} exponent={2}` (a true circle/stadium)
  on *any* size, `icon` included — ignored if `radius` or `exponent` is set
  explicitly, so it never fights a real override. Shape is orthogonal to
  size: an icon button defaults to the same soft-rounded corner as a text
  button; ask for `pill` (or `radius`/`exponent` directly) when a circular
  icon button is what you actually want (e.g. an avatar-style trigger) —
  don't assume every icon button is round.
- **`size`**: `sm`/`md`/`lg`/`icon` set a *starting* `gap`/padding/text-size
  className (`SIZE_CLASSES` in `buttons.tsx`) — a default, not a cap. There's
  no forced width/height via inline style anywhere in `Button`, so
  overriding any of it — `className="px-10 text-lg"`, a taller custom
  button, whatever — always wins via the normal `cn()`/twMerge conflict
  resolution instead of silently losing to a hardcoded value. Content sizes
  the button naturally; nothing truncates or clips long or short labels.
- **`href` auto-detects internal vs. external**: an internal path renders a
  Next `Link` (client-side nav, same tab); anything matching
  `isExternalHref()` (`http(s)://`, `mailto:`, `tel:`, protocol-relative
  `//`) renders a plain `<a target="_blank" rel="noopener noreferrer">`. No
  `href` at all renders a native `<button>`. One component, three render
  modes — don't reach for a parallel "internal-only" button like the old
  `PillLink` again; fix/extend `Button` instead. `Button` doesn't auto-append
  an outbound-arrow icon for external links either — not every button that
  happens to have an external `href` wants one signaled; pass one explicitly
  via `trailingIcon` when it's actually wanted.
- Icon-only buttons (`size="icon"`) get their own coloring regardless of
  variant — muted at rest, full contrast on hover:
  `text-[#a1a1a1] group-hover:text-[#1d1d1d] dark:text-[#646464] dark:group-hover:text-[#f0f0f0]`.
- **`border`/`borderColor`/`strokeWidth`** — a `Squircle` stroke isn't tied to
  `outline` anymore; any variant can have one. `border` defaults to `true`
  for `outline` and `secondary`, `false` otherwise, but is an explicit
  boolean override either way — a `solid` button with a border, or an
  `outline` button with `border={false}`, both just work. `borderColor`
  defaults to the shared `--border-hairline` token; `strokeWidth` defaults
  to `1`.
- **`background`/`textColor` (+ `darkBackground`/`darkTextColor`)** — the
  escape hatch for a one-off color a named variant doesn't cover. `background`
  replaces the currentColor-driven surface *entirely* (any valid CSS paint
  value — hex, a CSS var, even a gradient string; `Squircle` already has a
  clipped-div fallback for `linear-gradient(...)`/etc., see §4). Since a
  literal color has no `dark:` variant of its own the way a Tailwind class or
  a `.dark`-scoped CSS var does, `darkBackground`/`darkTextColor` are how to
  give it one — they fall back to `background`/`textColor` when omitted, and
  resolving between the pair is the *only* reason `Button` reads
  `useTheme()`/`useMounted()` at all (skipped entirely, no hook overhead,
  when neither prop is used). If a color should simply flip with `.dark`
  everywhere it's used, define a CSS custom property instead (see
  `--border-hairline`, §1) rather than passing `darkBackground` at every call
  site.
- Press feedback: `active:scale-[0.97] active:brightness-[1.04]
  transition-[color,scale,filter] duration-200`, plus `will-change-transform`
  so the compositing layer for that transform is ready before the first
  press rather than created on demand. Deliberately **no** `hover:scale-*` —
  scaling an element that contains text forces the browser to re-hint/
  re-rasterize the glyphs at a fractional pixel size on every frame of the
  transition, which reads as a visible stutter on hover. `:active` is brief
  enough that the same effect isn't noticeable, so scale/brightness stay
  there and hover is color-only. Not `transition-[transform,...]` either —
  see the v4 gotcha in §7, `scale-*` is the same story as `-translate-y-*`.
  Plain CSS throughout, not `whileHover`/`whileTap` — `Button` has no
  framer-motion dependency of its own, so it renders identically whether
  it's a `<button>`, `<a>`, or `Link`.
- `disabled` sets `pointer-events-none opacity-50` and, for link-mode,
  `aria-disabled`/`tabIndex={-1}` (an `<a>`/`Link` has no native `disabled`).
  Every render mode gets a visible `focus-visible:ring-2` — keyboard-focus
  states aren't optional.

**No `Magnetic` by default** — it's a special-occasion detail, not something
`Button` bakes in for every instance. If one specific button truly wants a
magnetic pull (a hero CTA, a social-link row), wrap that call site directly —
`<Magnetic><Button ...>` — rather than adding a prop that reintroduces it
everywhere. Composition over configuration here. `actionArea="global"`
(tracks the pointer anywhere on the page, not just on hover) is the version
to be most wary of — on anything clicked often, it reads as fidgety, not
delightful.

`Back`/`Copy` are thin, generic wrappers on top of `Button`
(`variant="secondary" size="icon"` by default), not one-off components:
`Back` takes an optional `href` and falls back to `router.back()` when
omitted (a "back" button doesn't always have one fixed destination), `Copy`
copies the current URL and is a reasonable "copy/share link" affordance for
any app. Everything else `Button` accepts — `variant`, `size`, `className`,
`radius`, `border`, all of it — passes straight through via `...rest`
(`IconButtonOverrides` in `buttons.tsx`), so neither one is a dead end if the
default look doesn't fit. This matters in particular for shrinking one below
the default icon size: `size="icon"`'s `min-h-10 min-w-10` (see above) is a
floor `className`'s own padding can't get under by itself — pass
`className="min-h-0 min-w-0 p-1"` (or whatever's wanted) to actually remove
it, the way the hero's inline `Copy` next to the terminal-command text does.

**A recurring question for anything new in `components/`**: is this
behavior specific to one site's content (a portfolio's "back to my
projects list", a fixed external social link), or is it a shape any app
would reuse? Keep the former out of shared components — put it in the
page/route that actually needs it — and design the latter so the *site*
supplies the specifics (an href, a label, an icon) while the component stays
opinion-free about what they are.

## 7. Motion (`motion/react`, package name `motion` — not the legacy
`framer-motion` alias)

Two named variants (`components/motion/variants.ts`), reused everywhere
lists/sections animate in — always name them `container`/`item`, never inline
one-offs:
```ts
container: { visible: { transition: { staggerChildren: 0.1 } } }
item: {
  hidden:  { opacity: 0, y: 12, filter: 'blur(12px)' },
  visible: {
    opacity: 1, y: 0, filter: 'blur(0px)',
    transition: { type: 'spring', damping: 25, stiffness: 200 },
    transitionEnd: { filter: 'none' },
  },
}
```

Interaction timing, by what's moving:
- Color/opacity hover: `duration-200 ease-out`.
- Larger movement (lift/position): `duration-300`–`400 ease-in-out`.
- Press feedback: `duration-150`.

**Tailwind v4 gotcha**: `-translate-y-*` and `scale-*` utilities set the
individual CSS `translate`/`scale` properties, not the combined `transform`.
A `transition-[transform,filter]` list silently misses both — list
`translate`/`scale` explicitly (whichever the hover/active state actually
uses) instead of reaching for `transform`.

## 8. Iconography

No icon registry or icon font. Two sources only:
- `lucide-react` — generic utility glyphs only (e.g. the theme switcher's
  `Monitor`/`Moon`/`Sun`, at `h-4 w-4`).
- Hand-authored inline SVGs everywhere else, `fill="currentColor"`, sized and
  colored with Tailwind classes directly on the `<svg>` tag.

## 9. Code Conventions

**Tailwind class ordering** — settled rule, not a suggestion:
1. Background (`bg-white`, `dark:bg-zinc-800`, `hover:bg-zinc-950`)
2. Border / shape (`border`, `rounded-md`, `ring-2`)
3. Typography (`text-sm`, `font-medium`, `tracking-normal`, `leading-snug`)
4. Text color (`text-black`, `dark:text-zinc-100`, `hover:text-zinc-50`)
5. Position (`absolute`, `relative`, `z-10`, `top-0`)
6. Flex/grid/size (`flex`, `items-center`, `w-full`, `gap-2`, `shrink-0`)
7. Everything else (`px-4`, `transition-all`, `duration-300`, `cursor-pointer`)

**Simple elements stay on one line** — a JSX element with no complex nested
logic (`<p className="...">{description}</p>`) shouldn't be broken across
three lines just because it has a long class string.

## 10. App Shell — Sidebar & Content Pane (`app/(main)/`)

`app/(main)/layout.tsx` composes two pieces, both in `_components/`:
`Sidebar` (the nav rail) and `ContentPane` (the main content area). Every
page under `(main)` — `/components/*`, `/app`, anything added later — gets
this shell automatically; `(main)` has no `page.tsx` of its own, since that
would collide with `(marketing)`'s at `/` (two route groups resolving the
same path is a build error). The sidebar app's own pages live under
`(main)/app/...`; the component-showcase pages live under
`(main)/components/*` — see §11.

- **Two-state collapse, not free-drag resize (desktop).** `Sidebar` toggles
  between a fixed icon-rail width (`w-16`) and a fixed expanded width
  (`w-64`) via one `transition-[width]` class — no mousemove-driven drag
  handle. A prior from-scratch sidebar (in a sibling project, not this one)
  implemented free resize with a ref-mutated width on the `<aside>` mirrored
  into a *second*, separately-positioned nav element's `left`/`width` via
  duplicated math on every pointer-move event — two independent DOM writes
  that can fall a frame out of sync, which is what actually read as
  "jittery" there. This implementation has no second element to keep in
  sync at all: the content pane is a plain `flex-1` sibling in the same flex
  row, so it always exactly fills the remaining space with zero manual
  width math, automatically in lockstep with the sidebar's own width
  transition.
- **Icon position is never conditional on `collapsed`, on purpose.** Every
  icon-bearing row (`SidebarItem`, the header's toggle button, the footer
  slot) uses one constant padding value in both states, chosen to exactly
  fill the rail's known *collapsed* width: `md:w-16` (64px) minus `nav`'s
  `p-2` (8px × 2) leaves 48px for `SidebarItem`, and a 16px icon in `px-4`
  (16px × 2) accounts for exactly those 48px — so it's centered with equal
  margins in the collapsed rail by construction, not by eyeballing it. Same
  arithmetic gives the header/footer icons `px-3` against their 40px
  `Button`s. Using that *same* value in the expanded state too (rather than
  a smaller "normal list" padding) means the padding never changes between
  states at all — nothing to transition, nothing that can visibly move,
  full stop. Two earlier versions tried conditional values here instead:
  first `justify-center` (snaps instantly, since browsers don't interpolate
  it, so the icon jumped straight to the center of whatever width the
  `<aside>` happened to be at that instant rather than easing there), then
  an *animated* padding change between two different values (smoother, but
  still a real, if small, position shift on every toggle). Only the
  trailing label's presence and the container's own width change now — the
  icon itself is simply never touched.
- **`SidebarItem` also pins its own row height (`min-h-9`), for the same
  reason.** Without it, the row's height comes from its tallest content —
  the label's `text-sm` line-height (20px) when present, vs. the bare 16px
  icon when it isn't — so hiding the label on collapse shrank the row by a
  few px on every toggle, moving the icon vertically the same way the
  padding issue above moved it horizontally. `min-h-9` (36px) is a no-op
  when the label's line-height already clears it and a floor when the icon
  alone wouldn't, so the row — and the icon inside it — is exactly as tall
  in both states.
- **Labels are a plain conditional render, not an `AnimatePresence`/
  `motion.span` fade.** A Framer Motion exit animation and the CSS
  `transition-[width]` are two independent animation engines with no shared
  clock — running both for the same collapse/expand gesture is the same
  "two systems that can fall out of step" problem the free-drag write-up
  above already calls out, just between two animation libraries instead of
  two DOM writes. Labels disappear the instant `collapsed` flips; the width
  transition alone carries the perceived motion.
- **Mobile**: below the `md` breakpoint a persistent icon rail has no useful
  width to collapse to — there's no room for both a rail and real content on
  a phone. `Sidebar` is fully off-canvas by default there and slides in as a
  `fixed inset-y-0 left-0` overlay animated via `transform` (a compositor-
  only property, no layout recalculation per frame, unlike the desktop
  `width` transition) with a `bg-black/40` backdrop. A `md:hidden` hamburger
  trigger reopens it — the toggle button inside the sidebar itself is
  unreachable while off-canvas, and on mobile it closes the drawer instead
  of collapsing to a rail (there's no rail state on mobile). The drawer also
  auto-closes on navigation, via `useMediaQuery` (`hooks/use-media-query.ts`,
  same `useSyncExternalStore` shape as `useMounted`) gating which behavior
  applies, and a render-time comparison against the last-seen `pathname` —
  see the hook and component source for why this isn't a `useEffect`.
  **Gotcha**: `useMediaQuery`'s `subscribe`/`getSnapshot` must be
  `useCallback`-memoized on the query string. Passing new inline arrow
  functions on every render made React tear down and rebuild the
  `matchMedia` subscription every render instead of once, which was enough
  to leave the hook permanently stuck on the server/hydration snapshot
  (`false`) instead of ever picking up the real client value — a real bug
  hit and fixed during this component's own build, not a hypothetical.
- **Color**: the sidebar is the secondary-surface tier (`#f0f0f0`/`#171717`,
  same as `variant="secondary"` buttons), visually distinct from the content
  pane's plain `#fefefe`/`#121212` background tier — deliberately not the
  same tone the way a stock ChatGPT-style sidebar and its content pane read
  as one flat surface. A hairline border (`--border-hairline`) separates the
  two.
- **The content pane is full-bleed, not a floating card.** `ContentPane` is
  a plain `<main>`, flush against the browser window's top/right/bottom
  edges — no margin, no padding, no rounded corners. An earlier version
  wrapped it in a `Squircle` with a `drop-shadow` for a floating-card look;
  that meant `Squircle`'s `ResizeObserver` recomputing and repainting an SVG
  corner path on every frame of the sidebar's width transition, stacked on
  top of a `drop-shadow` filter repaint on those same frames — real,
  avoidable per-frame cost sitting directly on top of the sidebar's own
  layout-triggering width transition, and a likely contributor to the
  collapse/expand animation reading as jittery. Removing the card fixed
  that as much as it changed the look.
- **The shadow is one-sided**, only on the edge facing the sidebar — a plain
  `box-shadow` with a negative x-offset and no y-offset
  (`shadow-[-12px_0_24px_-12px_rgba(28,29,33,0.18)]`), not a shadow on all
  four sides. The other three edges are the viewport's own edges (no margin
  to cast a shadow into), so this is really just "don't waste a shadow layer
  on edges nothing can see it against." A plain `box-shadow` works safely
  here specifically *because* the pane is a plain rectangle again — the
  Squircle/box-shadow incompatibility in §4/§5 no longer applies once
  there's no superellipse edge to clip against. Disabled entirely in dark
  mode (`dark:shadow-none`), not dimmed — same card-idiom convention as
  everywhere else; the sidebar's own hairline border is what still separates
  the two surfaces there.
- **Data-driven, not hardcoded**: `Sidebar` takes `items` (pinned top
  actions), `children` (the scrollable, app-specific middle region —
  `.no-scrollbar`, so it hides the scrollbar without removing scrolling),
  and `footer` (account menu, `ThemeToggle`, whatever). None of that content
  lives in `Sidebar` itself — same recurring-question test as §6: this
  component owns the shape/behavior, the call site supplies the content.
- `MainLayout` is a client component (`"use client"`) despite doing no
  client-only work of its own — it constructs `items` with real
  `LucideIcon` component references, which can't cross a Server → Client
  Component boundary as props (React can only serialize plain data across
  that boundary, not functions). `children` still passes through fine —
  already-rendered content from further down the tree works the same way
  through a client layout as through a server one.

## 11. Component Showcase (`app/(main)/components/*`, `components/layout/showcase-grid.tsx`)

Every design-system atom and every imported transitions-dev demo gets its
own sidebar category and its own page — `typography`, `buttons`, `squircle`,
`shadow`, `transitions` — rather than one long scrolling reference page.
Each page is a scoped list of `ShowcaseTileDef`s (`title`, `description`,
`children`, and two style escape hatches — `stageClassName`/
`stageExtraClassName`, see below) handed to `<ShowcaseGrid tiles={...} />`.
No numbered `Header` sections here — that convention stays on marketing-
adjacent reference material, not a nav-driven page per topic.

- **The grid reuses the marketing hero's decorative language, but isn't a
  blind copy of it** (`components/layout/grid-lines.tsx` —
  `VerticalLine`/`HorizontalLine`/`GridCircle`, extracted from `hero.tsx`
  so both places stay visually identical instead of two hand-tuned copies
  drifting apart). Explicitly **not** the bordered/shadowed "card" grid
  from the reference screenshot that prompted this — no
  `bg-*`/`border`/`shadow` anywhere in `showcase-grid.tsx`. Up to 3 tiles
  per row; each row is treated like one of hero's stacked sections — a
  `HorizontalLine` above it, `VerticalLine`s scoped to that row's own
  height for column dividers (`side="left"` on the 2nd/3rd tile — the same
  component, just called once per row instead of once for the whole page).
  Two real differences from hero, both load-bearing, not stylistic:
  - **Every line here passes `animate={false}`.** Hero's `whileInView`
    grow-in entrance depends on an `IntersectionObserver` watching the real
    browser viewport — correct for hero, a normal top-level scrolling
    page, but this grid renders inside the app shell's *nested*
    `overflow-y-auto` content pane (`content-pane.tsx`), and
    `whileInView` doesn't reliably fire/complete against a nested scroll
    container the way it does against the window. Lines were getting
    stuck a few px into their grow animation — never reaching full
    width/height — which is exactly what read as dividers being randomly
    "missing" between some columns/rows. `animate={false}`
    (`grid-lines.tsx`) renders the fully-grown end state directly, no
    observer involved, so every real boundary between adjacent tiles has
    a visible line, unconditionally.
  - **Circles go on all 4 outer corners of the grid, not 2.** Hero only
    needed 2 (top-left/bottom-right) for one decorative accent on an
    elongated single column; a wide multi-row grid reads as more
    intentionally "framed" with all 4. They're anchored to the outer
    wrapper (`GridCircle corner="..."`, not per-row/per-tile) specifically
    so a partial last row (fewer than 3 tiles) still puts the bottom
    corners at the grid's actual right/bottom edge, not wherever the last
    real tile's own cell happens to end.
- **Not every tile is the same size.** `span: 2` widens a tile to 2 of the
  row's 3 columns — for a demo that's genuinely wider than a third of the
  grid (a search bar, a profile card), not a default to reach for. Chunking
  is slot-based, not `items.length / 3`: a `span: 2` tile counts as 2 toward
  each row's 3-slot budget, so it pairs with exactly one `span: 1` tile, or
  sits alone (with a gap) if the remaining slot can't fit another wide one.
- **`stageExtraClassName`** is how a tile reaches into a demo's own
  hardcoded internal class names (`[&_.t-resize]:bg-...`) — several
  imported components render an otherwise-invisible or browser-default-
  styled element (a bare positioned `<div>`, an unstyled dropdown box, a
  plain `<input>`) since they ship zero surface styling of their own by
  design (bring-your-own-chrome snippets). `ShowcaseGrid` also applies one
  baseline rule to every tile automatically: `[&>button]` (direct-child-only)
  styling for each demo's own top-level trigger button, since 19/19 of the
  transitions-dev components either render their trigger as a direct
  sibling inside a bare Fragment or *are* the returned root button
  themselves — verified against all 19 before relying on it, since a
  broader `[&_button]` (all-descendants) selector would have also caught
  nested buttons that need their own distinct styling (a modal's "Close", a
  dropdown's menu items).
- **`components/transitions-dev/*` are vendored, not this template's own
  design system.** Copied as-is from a sibling project's `transitions-dev`
  folder (itself sourced from transitions.dev) — self-contained snippets,
  each injecting its own scoped `<style>` tag into `<head>` on first import,
  each `// @ts-nocheck`. `eslint.config.mjs` ignores this whole directory
  for the same reason `nextjs.org/**` (the scraped marketing reference) is
  ignored — vendored code, not ours to hold to this repo's lint/style
  conventions. Four *narrow, explicitly-commented* deviations from the
  original source were still necessary, each noted in the affected file's
  own header comment, not just here — two are bug fixes, two are additive:
  - `skeleton-reveal.tsx` had a genuine syntax bug in the upstream
    snippet — a comment inside its injected CSS template literal used
    markdown-style backticks around `.is-resetting`, which (since the
    whole styles block is itself a JS template literal) closed it early
    and threw a `ReferenceError` at module evaluation, crashing on import.
    Confirmed present in the original source, not introduced by copying it
    here. Fixed by rewriting the comment in plain text.
  - `clear-input.tsx` checked `document.documentElement.getAttribute
    ('data-theme')` to pick its glow color, assuming a data-attribute theme
    toggle; this template's `ThemeProvider` uses `attribute="class"` (see
    `app/layout.tsx`), so that check always read `null` and the glow
    silently rendered as light-mode regardless of the real theme. Changed
    to check for the `dark` class instead.
  - `menu-dropdown.tsx` originally took zero props — "Item 1"/"Item 2"
    were hardcoded, so there was no way to show a real, context-appropriate
    menu without forking the file. Added an optional `items` prop
    (`{ label, icon? }[]`); omitting it falls back to the original two
    hardcoded items, so this is additive, not a behavior change for
    anything already using it as shipped.
  - `number-loop.tsx` only ever replayed the *same* value on every click —
    barely a "pop in" demo if the value it's transitioning to never
    differs. Added an optional `onBeforeReplay` callback, fired
    synchronously at the top of `replay()`, before the class gets
    stripped for the reflow; a caller can change `value` from its own
    state inside that callback (React batches it with this component's
    own `setPlaying(false)`), so new digits are already in place when the
    entrance re-triggers. Omitting it preserves the original
    replay-same-value behavior exactly. (Also worth knowing, not a
    deviation: the CSS only defines stagger delays for the first three
    digits — pick ~3-character values, longer ones pop in together past
    that point.)
  - Everything else — behavior, structure, styling philosophy (bring your
    own colors/sizing) — is untouched. Dark-mode gaps that are cosmetic
    rather than broken (e.g. `text-shimmer.tsx`'s hardcoded highlight color,
    `tooltip.tsx`'s hardcoded light background) are patched per-demo from
    the *showcase* side instead, via `stageExtraClassName`'s
    `dark:[--css-var:value]` overrides — the vendored file's own CSS
    variables stay as shipped; only the value read in dark mode is
    overridden, scoped to that one tile.

## Not yet built (later phases of this template, not this pass)

`actions/`, `db/`, auth.
